# Variables de entorno

# ── SSH ──
SSH_HOST=ip
SSH_PORT=22
SSH_USER=user
SSH_PASSWORD='password'
SSH_KEY_PATH=
# Ruta absoluta a llave privada (alternativa a password)

# ── MySQL ──
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=user
DB_PASSWORD='password'
DB_NAME=mi_base_de_datos

# ── Logging ──
LOG_LEVEL=INFO

# ── Healthcheck ──
HEALTHCHECK_TABLE=tabla
